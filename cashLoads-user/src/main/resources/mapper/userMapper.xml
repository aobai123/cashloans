<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.aobai.user.mapper.UserMapper">
  <select id="selectList" resultType="User">
        SELECT
            id,
            user_name,
            education,
            address,
            detailed_address,
            live_time,
            marital_status,
            qq,
            id_card,
            immediate_family,
            immediate_name,
            immediate_phone,
            colleagues_name,
            colleagues_phone
        FROM
            t_user
    </select>

</mapper>